import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SearchedResult = () => {
  return (
    <View>
      <Text>SearchedResult</Text>
    </View>
  );
};

export default SearchedResult;

const styles = StyleSheet.create({});

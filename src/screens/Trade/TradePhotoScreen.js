import { StyleSheet, Text, View } from "react-native";
import React from "react";

const TradePhotoScreen = () => {
  return (
    <View>
      <Text>TradePhotoScreen</Text>
    </View>
  );
};

export default TradePhotoScreen;

const styles = StyleSheet.create({});
